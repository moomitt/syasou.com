<div class="container-fluid public">
  <div class="row">
    <div class="col-12 mx-auto text-center">
      <h4 class="heading my-5">
        新規登録
      </h4>
    </div>

    <div class="col-lg-10 col-md-10 col-10 mx-auto bg-light pt-4" style="max-width: 500px;">
      <%= form_with model: @user, url: user_registration_path do |f| %>
        <div class="row input-group-sm ml-4 mr-3 mb-4 text-danger">
          <% if @user.errors.any? %>
            <p class="col-12">登録エラー</p>
            <ul class="col-12 pl-5">
              <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          <% end %>
        </div>

        <div class="row input-group-sm d-flex align-items-center my-3">
          <label class="label-control col-10 mx-auto px-0">
            ニックネーム<small>（2～10文字）</small>
          </label>
          <%= f.text_field :name, autofocus: true, autocomplete: "name",
          class: "form-control col-10 mx-auto"  %>
        </div>

        <div class="row input-group-sm d-flex align-items-center my-3">
          <label class="label-control col-10 mx-auto px-0">メールアドレス</label>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
          class: "form-control col-10 mx-auto"  %>
        </div>

        <div class="row input-group-sm d-flex align-items-center my-3">
          <label class="label-control col-10 mx-auto px-0">
            パスワード
            <% if @minimum_password_length %>
              <small>（<%= @minimum_password_length %> 文字以上）</small>
            <% end %>
          </label>
          <%= f.password_field :password, autocomplete: "new-password",
          class: "form-control col-10 mx-auto"  %>
        </div>

        <div class="row input-group-sm d-flex align-items-center my-3">
          <label class="label-control col-10 mx-auto px-0">
            パスワード<small>（確認用）</small>
          </label>
          <%= f.password_field :password_confirmation, autocomplete: "new-password",
          class: "form-control col-10 mx-auto"  %>
        </div>

        <div class="row my-5">
          <%= f.submit "新規登録", class: "btn btn-sm btn-success col-10 mx-auto"  %>
        </div>
      <% end %>
    </div>

    <div class="col-12 my-5 mx-auto text-center">
      <h5 class="my-4">
        会員登録がお済みの方
      </h5>
      <p class="my-4">
        <%= link_to 'こちら', new_user_session_path, class: "text-main" %>
        からログインしてください。
      </p>
    </div>

  </div>
</div>





